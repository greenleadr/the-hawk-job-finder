# Hawk Job Finder — Project State

> Last updated: 2026-04-02

## Current Iteration: v0.1 (Initial Setup)

### What Works
- **Career pages collector**: Queries Greenhouse, Lever, and Ashby public JSON APIs. Filters to target titles. Returns full job descriptions for better scoring.
- **Adzuna collector**: Queries title variants across locations, rate limiting, retry
- **Remotive collector**: Queries product category, filters to senior titles, strips HTML
- **Scoring engine**: 0-100 scale across title/skills/experience/industry with penalties
- **HTML digest**: Styled email with score bars, skill pills, flag badges, top-10 ranking
- **Email delivery**: Brevo SMTP
- **SQLite persistence**: Dedup by SHA-256 hash, upsert, history queries
- **GitHub Actions**: Daily cron at 7am PT + manual dispatch, commits jobs.db back to repo
- **Profile loading**: Supports both local file and PROFILE_JSON env var for CI

### What's Stubbed
- `output/` directory — Referenced in workflow artifacts but not yet generated by pipeline

### Known Issues
- **Remotive returns 0 product jobs**: The category endpoint sometimes has no listings. Not a bug — depends on their inventory.
- **Workday/Rippling companies** not queryable: No public JSON API. These companies are skipped by career_pages collector.

## Component Status

| Component | Status | Notes |
|-----------|--------|-------|
| `profile.json` | Done | 15 target titles, 44 skills + variants, 4 roles, Nadine's full resume context |
| `companies.json` | Needs review | Inherited from template — review for Nadine's target companies |
| `collectors/adzuna.py` | Done | Title queries, rate limiting, retry |
| `collectors/remotive.py` | Done | Title filter regex, HTML stripping |
| `collectors/career_pages.py` | Done | Greenhouse, Lever, Ashby public JSON APIs |
| `scorer.py` | Done | 5 scoring dimensions, batch scoring, demo |
| `scorer_llm.py` | Done | Claude Haiku, Tier 1 ≥50 threshold, Apply/Maybe/Skip |
| `digest.py` | Done | Inline CSS, score colors, skill/flag pills |
| `emailer.py` | Done | Brevo SMTP |
| `db.py` | Done | SQLite, SHA-256 dedup, upsert, history |
| `main.py` | Done | 10-step pipeline, env var config |
| `daily.yml` | Done | Cron 7am PT, workflow_dispatch, DB commit |

## Configuration

### Secrets (GitHub Actions)
| Secret | Status | Notes |
|--------|--------|-------|
| `ADZUNA_APP_ID` | Needs setup | developer.adzuna.com |
| `ADZUNA_API_KEY` | Needs setup | developer.adzuna.com |
| `BREVO_SMTP_LOGIN` | Needs setup | Brevo account email |
| `BREVO_SMTP_KEY` | Needs setup | Brevo SMTP password |
| `EMAIL_TO` | Needs setup | Nadine's recipient address |
| `PROFILE_JSON` | Needs setup | Full profile as JSON string |
| `BREVO_SENDER` | Needs setup | Sender address (configure after setting up domain) |
| `ANTHROPIC_API_KEY` | Needs setup | Required for LLM scoring. Auto-enables USE_LLM_SCORING. |

### DNS / Email
- Domain: Not yet configured
- DKIM: Not yet configured
- DMARC: Not yet configured
- Sender: Update `BREVO_SENDER` secret and `DEFAULT_SENDER` in emailer.py once domain is set up

## Next Steps (Priority Order)
1. **Review `companies.json`** — Curate target companies relevant to Nadine's SWE job search
2. **Configure GitHub Actions secrets** — Set up all required API keys and credentials
3. **Set up email sender domain** — Configure Brevo SMTP with verified domain
4. **Test pipeline** — Run `SKIP_EMAIL=true python main.py` to verify end-to-end
